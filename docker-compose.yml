services:
  t-invest-bot:
    env_file: .env
    build:
      context: .
      dockerfile: Dockerfile
    container_name: t-invest-bot
    hostname: t-invest-bot
    pull_policy: always
    networks:
      - default
    volumes:
      - /Users/user/docker/data/t-invest-bot:/app/data
